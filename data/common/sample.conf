# Application runtime configuration

[server]
listen_address = 0.0.0.0
listen_port    = 8080

[logging]
level          = info
format         = json

[features]
new_ui         = true
rate_limit     = 500
